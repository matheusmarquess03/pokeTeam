<div class="table-responsive">
  <table class="table align-middle">
    <thead>
      <tr>
        <th scope="col"><%= t(:name, scope: 'activerecord.attributes.team') %></th>
        <th scope="col"><%= t(:trainer, scope: 'activerecord.attributes.team') %></th>
        <th scope="col"><%= t(:quantity_pokemon, scope: 'activerecord.attributes.team') %></th>
        <th scope="col"><%= link_to "Criar Time", new_team_path,  class: "btn btn-info", style: "margin-top: -5px margin-right: 10px" %></th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% teams.each do |team| %>
      <tr class="align-bottom">
        <td><%= team.name %></td>
        <td>
          <%= link_to profile_path(team.trainer) do %>
            <%= team.trainer.name %>
          <% end -%>
        </td>
        <td><%= team.pokemons.count %></td>
        <td class="align-middle">
          <%= link_to 'Exibir', team_path(team),  class: "btn btn-success btn-sm", style: "margin-top: -5px" %>
        </td>
        <% if current_trainer == team.trainer %>
          <td class="align-middle">
            <%= link_to 'Editar', edit_team_path(team),  class: "btn btn-warning btn-sm", style: "margin-top: -5px" %>
          </td>
          <td class="align-middle">
            <%= link_to team, method: :delete,
                data: { confirm: 'Você tem certeza que deseja excluir?' },
                class: "btn btn-danger btn-sm", style: "margin-top: -5px" do %>
                 </i> Excluir
                <% end %>
          </td>
        <% end -%>
      </tr>
    <% end -%>
    </tbody>
  </table>
</div>